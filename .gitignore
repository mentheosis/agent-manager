__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
*.log

# Local state
state.json
.agent-manager/

# Per-developer docker-compose overrides (mount your own host paths)
xdocker-compose.local.yml
xDockerfile.local
requirements.local.txt
constraints.local.txt

# docker-mcp (host-side binary and user config)
docker-mcp/am-docker-mcp
docker-mcp/config.json

# Local-only example configs, host-specific scripts, and ngrok policy
config_examples/
docker-mcp/scripts/
ngrok-policy.yml
